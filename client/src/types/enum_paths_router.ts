enum PathsRouter {
  ADMIN_ROUTER = '/admin',
  LOGIN_ROUTER = '/login',
  REGISTRATION_ROUTER = '/registration',
  SHOP_ROUTER = '/',
  BASKET_ROUTER = '/basket',
  DEVICE_ROUTER = '/device',
}

export default PathsRouter;